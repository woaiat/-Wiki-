diff --git "a/raw/前端/第一章笔记.md" "b/raw/前端/第一章笔记.md"
new file mode 100644
--- /dev/null
+++ "b/raw/前端/第一章笔记.md"
@@ -0,0 +1,174 @@
+# 第一章笔记：HTML5 页面骨架
+
+## 1. 本章核心
+
+- HTML 负责网页的结构和内容，不负责页面美化。
+- 这一章的重点是：页面基本结构、`<head>`、`<body>`、常用标签、语义化标签。
+- 可以先把网页理解成一栋房子：HTML 先把骨架搭好，后面的 CSS 再负责装修，JavaScript 再负责交互。
+
+## 2. 定义与原理
+
+### 2.1 什么是 HTML
+
+HTML 是 `HyperText Markup Language`，中文一般叫“超文本标记语言”。
+它的作用不是“编程计算”，而是用不同的标签把网页内容组织起来，让浏览器知道：哪里是标题，哪里是正文，哪里是图片，哪里是链接。
+
+### 2.2 HTML5 页面为什么必须有固定骨架
+
+一个完整的 HTML5 页面通常都遵循固定结构，因为浏览器需要先知道：
+
+- 这是不是 HTML5 文档。
+- 整个网页从哪里开始、哪里结束。
+- 哪些内容是给浏览器看的信息。
+- 哪些内容是给用户直接看的内容。
+
+如果没有这套骨架，浏览器虽然有时也能“猜着显示”，但网页结构会不规范，也容易出问题。
+
+## 3. 最小 HTML5 页面结构
+
+```html
+<!DOCTYPE html>
+<html>
+<head>
+  <meta charset="UTF-8">
+  <title>我的第一个页面</title>
+</head>
+<body>
+  <h1>欢迎</h1>
+  <p>这里是正文。</p>
+</body>
+</html>
+```
+
+### 3.1 各部分含义
+
+- `<!DOCTYPE html>`：声明这是 HTML5 文档，并告诉浏览器使用标准模式。
+- `<html>`：根标签，整张网页都包在里面。
+- `<head>`：存放页面的基础信息，例如编码、标题、CSS、JavaScript。
+- `<title>`：显示在浏览器标签页上的标题。
+- `<body>`：存放用户真正能看到的内容，例如文字、图片、链接、表格、表单等。
+
+### 3.2 文字化理解
+
+可以把网页分成两层：
+
+- `head` 是“后台说明书”，主要给浏览器看。
+- `body` 是“前台展示区”，主要给用户看。
+
+这个区分非常重要，因为考试里最常见的问题之一就是：哪些标签应该放在 `head` 中，哪些内容应该写在 `body` 中。
+
+## 4. `<head>` 部分重点
+
+`<head>` 中常见的内容有：
+
+- `<title>`：网页标题。
+- `<meta charset="UTF-8">`：指定字符编码，中文页面很常用。
+- `<meta name="description" content="...">`：页面描述。
+- `<meta name="keywords" content="...">`：关键词。
+- `<link rel="stylesheet" href="styles.css">`：引入外部 CSS。
+- `<script src="app.js"></script>`：引入外部 JavaScript。
+
+### 4.1 为什么 `meta charset="UTF-8"` 很重要
+
+如果页面里有中文，但没有正确设置字符编码，就可能出现乱码。
+所以在中文网页里，`UTF-8` 基本可以看成一个非常常见的标准配置。
+
+## 5. `<body>` 部分与常用标签
+
+`<body>` 里放的是页面可见内容。常见标签包括：
+
+- `<h1>` 到 `<h6>`：标题标签，从大到小。
+- `<p>`：段落。
+- `<br>`：换行。
+- `<a href="...">`：超链接。
+- `<img src="..." alt="...">`：图片。
+- `<ol>`、`<ul>`、`<li>`：列表。
+- `<div>`：块级容器。
+- `<details>` 和 `<summary>`：折叠内容。
+- `<iframe>`：在当前页面中嵌入另一个网页。
+
+## 6. 语义化标签
+
+### 6.1 定义
+
+语义化标签就是“标签名本身就能表达内容作用”的标签。
+它不仅让代码更清楚，也更利于搜索引擎理解网页结构，同时对无障碍访问更友好。
+
+### 6.2 常见语义化标签
+
+- `<header>`：页面或区块开头。
+- `<nav>`：导航区域。
+- `<article>`：独立内容，例如文章、新闻。
+- `<section>`：主题分区。
+- `<aside>`：侧边补充内容。
+- `<footer>`：页脚。
+
+### 6.3 文字化理解
+
+如果只用 `<div>` 堆页面，浏览器能显示，但代码语义不清楚。
+如果用 `<header>`、`<nav>`、`<article>` 这些标签，别人一看代码就知道每一块在做什么。
+这就是“语义化”的意义。
+
+## 7. 例子与例题
+
+### 7.1 例子：有序列表和无序列表的区别
+
+```html
+<ol>
+  <li>起床</li>
+  <li>刷牙</li>
+  <li>吃早饭</li>
+</ol>
+
+<ul>
+  <li>苹果</li>
+  <li>香蕉</li>
+  <li>橙子</li>
+</ul>
+```
+
+解释：
+
+- `ol` 表示有序列表，适合步骤、流程这类“顺序不能乱”的内容。
+- `ul` 表示无序列表，适合普通并列项目，顺序不重要。
+
+### 7.2 例题：下面哪些内容应该放在 `head` 中？
+
+题目：网页标题、正文文字、字符编码、CSS 文件链接，这四项中哪些应该放在 `head` 中？
+
+答案：
+
+- 网页标题：放在 `head` 中。
+- 字符编码：放在 `head` 中。
+- CSS 文件链接：放在 `head` 中。
+- 正文文字：不放在 `head` 中，应该放在 `body` 中。
+
+原因：
+
+因为 `head` 负责页面信息和资源声明，`body` 负责用户可见内容。
+
+### 7.3 例题：判断下列说法是否正确
+
+- `<!DOCTYPE html>` 是普通成对标签。
+- `<body>` 里可以写标题、图片、链接。
+- `<font>` 是 HTML5 语义化结构标签。
+
+答案：
+
+- 第一条错误。`<!DOCTYPE html>` 不是普通成对标签，而是文档类型声明。
+- 第二条正确。`body` 就是存放可见内容的区域。
+- 第三条错误。`<font>` 不是语义化结构标签，它属于旧式表现标签。
+
+## 8. 本章易错点
+
+- `head` 和 `body` 不要混淆：前者放信息，后者放内容。
+- `<br>` 只是换行，不是插入图片。
+- `<!DOCTYPE html>` 必须写在最前面。
+- 容器标签通常成对出现，例如 `<html></html>`；空标签通常没有结束标签，例如 `<br>`。
+- 不要把老式表现标签误认为语义化标签，例如 `<font>` 就不是语义化标签。
+
+## 9. 本章小结
+
+- HTML5 页面有固定骨架：`<!DOCTYPE html>`、`<html>`、`<head>`、`<body>`。
+- `head` 放页面信息和资源，`body` 放用户可见内容。
+- 常用标签和语义化标签是后续学习 CSS、JavaScript、Bootstrap、Vue 的基础。
